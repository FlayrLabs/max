<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Max.Windows.PillWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="Root" Padding="16" RowSpacing="12" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer x:Name="ChatScroll" Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ChatStack" Spacing="10" />
        </ScrollViewer>

        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                x:Name="Input"
                Grid.Column="0"
                PlaceholderText="Ask Max to do anything…"
                AcceptsReturn="False"
                KeyDown="Input_KeyDown"
                FontSize="16" />

            <Button
                x:Name="SendBtn"
                Grid.Column="1"
                Content="Send"
                Style="{StaticResource AccentButtonStyle}"
                Click="Send_Click" />
        </Grid>
    </Grid>
</Window>
